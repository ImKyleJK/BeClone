const ContentMiddleware = require('../common/middlewares/main.public.middleware');
const config = require('../common/config/env.config');

exports.routesConfig = function (app) {
    app.get('/:type/:file', [
        
    ]);
};